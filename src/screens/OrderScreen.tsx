import React from 'react';
import {Layout, Text} from 'react-native-ui-kitten';
import StyledBar from '../components/StyledBar';

const OrderScreen = props => {
  return (
    <Layout>
      <StyledBar>
        <Text>Virgo's Resurrection</Text>
      </StyledBar>
    </Layout>
  );
};

export default OrderScreen;
