import * as base from "./basic";

export const circle = {
  normal: {
    radius: base.radius.circle,
    backgroundColor: base.color.white,
    margin: base.margin.md
  }
};

export const icon = {
  header: {
    width: 20,
    height: 20,
    color: base.color.darkGrey
  }
};
